projects.dir = ${basedir}/..
wkspace.dir = ${projects.dir}/..
common.dir = ${wkspace.dir}/automation-build/common
test.buildfile.dir = ${wkspace.dir}/automation-build/test

main.lib.dir = ${wkspace.dir}/shared-lib/src/main/lib
project.lib.dir = ${wkspace.dir}/shared-lib/src/project/lib
test.lib.dir = ${wkspace.dir}/shared-lib/src/test/lib

target.dir = ${projects.dir}/target
classes.dir = ${target.dir}/classes

test.result.dir = ${target.dir}/test
junit.report.dir = ${test.result.dir}/junit
jacoco.report.dir = ${test.result.dir}/jacoco
test.classes.dir = ${test.result.dir}/classes

build.dir = ${projects.dir}/target
test.classes.dir = ${build.dir}/test/classes

lib.dir = ${wkspace.dir}/shared-lib/src/main/lib
project.lib.dir = ${wkspace.dir}/shared-lib/src/project/lib
test.lib.dir = ${wkspace.dir}/shared-lib/src/test/lib

test.src.dir = ${projects.dir}/src/test/java
